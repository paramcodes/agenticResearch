# Web frontend (Vite + React). Copy to .env for local dev.
VITE_API_URL="http://localhost:4000"
# Google login: same client id as the api's GOOGLE_CLIENT_ID (Web OAuth client).
# Leave empty to hide the Google button.
VITE_GOOGLE_CLIENT_ID=
